namespace Assurance.Logging;

/// <summary>
/// Names the library's own events, and prefixes the fields it writes into a caller's event.
/// </summary>
internal static class AssuranceLog
{
    public const string Component = "Assurance";
}
